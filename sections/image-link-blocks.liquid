{%- render 'section-spacing-collapsing' -%}

{%- comment -%}
------------------------------------------------------------------------------------------------------------------------
CSS
------------------------------------------------------------------------------------------------------------------------
{%- endcomment -%}
<style>
  #shopify-section-{{ section.id }} {
    padding-top: 44px !important;
  }

  #shopify-section-{{ section.id }} .image-link-blocks {
    --image-link-blocks-item-width: 60vw;
    --image-link-blocks-grid: auto / auto-flow var(--image-link-blocks-item-width);
    --content-over-media-gap: var(--grid-gutter);
  }

  #shopify-section-{{ section.id }} .image-link-blocks__item {
    aspect-ratio: 1 / 1 !important;
  }

  #shopify-section-{{ section.id }} .image-link-blocks__item .content-over-media,
  #shopify-section-{{ section.id }} .image-link-blocks__item .image-link-blocks__inner {
    aspect-ratio: 1 / 1 !important;
    height: 100%;
  }

  #shopify-section-{{ section.id }} .image-link-blocks__item img {
    object-fit: contain;
    object-position: right;
    width: auto;
    height: 100%;
    aspect-ratio: 1 / 1 !important;
  }

  @media screen and (min-width: 700px) {
    #shopify-section-{{ section.id }} .image-link-blocks {
      --image-link-blocks-item-width: calc((100vw - var(--container-gutter) * 2) / 3.5);
    }
  }

  @media screen and (min-width: 1000px) {
    #shopify-section-{{ section.id }} .image-link-blocks {
      --image-link-blocks-item-width: calc(var(--container-inner-width) / 3.5);
    }
  }

  /* Scallop button styling - inherits from circle-button positioning and hover behavior */
  #shopify-section-{{ section.id }} .circle-button.image-link-scallop-button {
    background: transparent !important;
    border: none !important;
    padding: 0 !important;
    cursor: pointer;
    display: inline-flex !important;
    align-items: center;
    justify-content: center;
    box-shadow: none !important;
    width: 51px !important;
    height: 51px !important;
    min-width: 51px !important;
    min-height: 51px !important;
  }

  #shopify-section-{{ section.id }} .circle-button.image-link-scallop-button svg {
    width: 51px !important;
    height: 51px !important;
    display: block;
    filter: none !important;
  }

  #shopify-section-{{ section.id }} p {
    margin-top: 16px;
    color: #828282;
    font-size: var(--text-base);
  }

  @media screen and (min-width: 700px) {
    #shopify-section-{{ section.id }} p {
      width: 60%;
    }
  }

  #shopify-section-{{ section.id }} .section-header a {
    font-size: 10px;
    font-weight: 500;
    text-decoration: underline;
    color: #000000;
    margin-top: 16px;
    display: inline-block;
  }

  @media screen and (min-width: 700px) {
    #shopify-section-{{ section.id }} .section-header a {
      font-size: 14px;
    }
  }

  #shopify-section-{{ section.id }} .section-header a .circle-chevron,
  #shopify-section-{{ section.id }} .section-header a .circle-chevron svg {
    display: none !important;
  }

  /* Link text styling */
  #shopify-section-{{ section.id }} .image-link-blocks__title,
  #shopify-section-{{ section.id }} .h-stack {
    font-size: 30px;
    font-weight: 600;
  }

  @media screen and (min-width: 700px) {
    #shopify-section-{{ section.id }} .image-link-blocks__title,
    #shopify-section-{{ section.id }} .h-stack {
      font-size: 36px;
    }
  }

  /* Move CTA to bottom */
  #shopify-section-{{ section.id }} .section-header {
    display: block !important;
    grid-template-columns: none !important;
    justify-content: unset !important;
    align-items: unset !important;
  }

  #shopify-section-{{ section.id }} .section-header a {
    margin-top: 24px;
    display: block;
  }

  @media screen and (min-width: 700px) {
    #shopify-section-{{ section.id }} .section-header a {
      margin-top: 36px;
    }
  }

  /* Link subtext styling */
  #shopify-section-{{ section.id }} .image-link-blocks__subtext,
  #shopify-section-{{ section.id }} .place-self-start p {
    font-size: 16px;
    margin: 0;
    color: #000000;
    opacity: 0;
    transition: opacity 0.5s ease;
  }

  @media screen and (min-width: 700px) {
    #shopify-section-{{ section.id }} .image-link-blocks__subtext,
    #shopify-section-{{ section.id }} .place-self-start p {
      font-size: 22px;
    }
  }

  #shopify-section-{{ section.id }} .image-link-blocks__item:hover .image-link-blocks__subtext,
  #shopify-section-{{ section.id }} .image-link-blocks__item:hover .place-self-start p {
    opacity: 1;
  }

  /* Graphic styling using ::before and ::after pseudo-elements */
  {%- for block in section.blocks -%}
    {%- if block.settings.graphic != blank -%}
      #block-{{ block.id }}.image-link-blocks__item a::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url('{{ block.settings.graphic | image_url: width: 400 }}');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        z-index: -1;
        pointer-events: none;
        opacity: 1;
        transition: opacity 0.5s ease;
      }
    {%- endif -%}
    
    {%- if block.settings.hover_graphic != blank -%}
      #block-{{ block.id }}.image-link-blocks__item a::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url('{{ block.settings.hover_graphic | image_url: width: 400 }}');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        z-index: -1;
        pointer-events: none;
        opacity: 0;
        transition: opacity 0.5s ease;
      }
    {%- endif -%}
  {%- endfor -%}

  /* Hover effect to switch graphics */
  {%- for block in section.blocks -%}
    {%- if block.settings.graphic != blank and block.settings.hover_graphic != blank -%}
      #block-{{ block.id }}.image-link-blocks__item:hover a::before {
        opacity: 0;
      }
      
      #block-{{ block.id }}.image-link-blocks__item:hover a::after {
        opacity: 1;
      }
    {%- endif -%}
  {%- endfor -%}

  /* Hover effect for image blocks */
  {%- for block in section.blocks -%}
    #block-{{ block.id }}.image-link-blocks__item:hover {
      background: {{ block.settings.hover_background_color | default: '#F9CEC6' }} !important;
      transition: background-color 0.5s ease;
    }
  {%- endfor -%}
</style>

{%- comment -%}
------------------------------------------------------------------------------------------------------------------------
LIQUID
------------------------------------------------------------------------------------------------------------------------
{%- endcomment -%}

<div {% render 'section-properties' %}>
  <div class="section-stack">
    {%- render 'section-header', subheading: section.settings.subheading, heading: section.settings.title, heading_color: section.settings.heading_color, heading_gradient: section.settings.heading_gradient, content: section.settings.content, link_text: section.settings.link_text, link_url: section.settings.link_url -%}

    <div class="floating-controls-container">
      {%- assign scroll_area_id = 'scroll-area-' | append: section.id -%}

      <image-link-blocks id="{{ scroll_area_id }}" class="image-link-blocks scroll-area bleed">
        {%- for block in section.blocks -%}
          {%- liquid
            assign loading_strategy = nil
  
            if section.index > 3 or forloop.index > 3
              assign loading_strategy = 'lazy'
            endif
          -%}

          <div id="block-{{ block.id }}" {% render 'surface', class: 'image-link-blocks__item group rounded', background: block.settings.background, text_color: block.settings.text_color, background_fallback: 'bg-secondary' %} {{ block.shopify_attributes }}>
            {%- if block.settings.stretch_image -%}
              {%- capture item_content -%}
                {%- if block.settings.image != blank -%}
                  {{- block.settings.image | image_url: width: block.settings.image.width | image_tag: loading: loading_strategy, sizes: '(max-width: 699px) 160px, 285px', widths: '200,300,400,500,600,700,800,900,1000,1200', class: 'zoom-image' -}}
                {%- else -%}
                  {{- 'image' | placeholder_svg_tag: 'zoom-image placeholder' -}}
                {%- endif -%}

                {%- if block.settings.link_subtext != blank -%}
                  <div class="place-self-start w-full">
                    <p>{{ block.settings.link_subtext }}</p>
                  </div>
                {%- endif -%}

                {%- if block.settings.link_text != blank -%}
                  <div class="place-self-end-start w-full bold">
                    <div class="h-stack justify-between gap-2">
                      {{ block.settings.link_text | escape }}

                      {%- if block.settings.link_url != blank -%}
                        {%- if block.settings.stretch_image -%}
                          {% render 'icon' with 'circle-button-right-clipped', direction_aware: true %}
                        {%- else -%}
                          <span class="circle-chevron">{% render 'icon' with 'chevron-right-small', direction_aware: true %}</span>
                        {%- endif -%}
                      {%- endif -%}
                    </div>
                  </div>
                {%- endif -%}

              {%- endcapture -%}

              <a {% if block.settings.link_url %}href="{{ block.settings.link_url }}"{% endif %} class="content-over-media rounded" style="--content-over-media-overlay: {{ block.settings.overlay_color.rgb }} / {{ block.settings.overlay_opacity | divided_by: 100.0 }}">
                {{- item_content -}}
              </a>
            {%- else -%}
              <a {% if block.settings.link_url != blank %}href="{{ block.settings.link_url }}"{% endif %} class="image-link-blocks__inner">
                {%- if block.settings.image != blank -%}
                  {{- block.settings.image | image_url: width: block.settings.image.width | image_tag: loading: loading_strategy, sizes: '(max-width: 699px) 160px, 285px', widths: '200,300,400,500,600,700,800,900,1000,1200', class: 'image-link-blocks__image zoom-image' -}}
                {%- else -%}
                  {{- 'image' | placeholder_svg_tag: 'image-link-blocks__image placeholder' -}}
                {%- endif -%}

                {%- if block.settings.link_subtext != blank -%}
                  <p class="image-link-blocks__subtext text-center">{{ block.settings.link_subtext }}</p>
                {%- endif -%}

                {%- if block.settings.link_text != blank -%}
                  <p class="image-link-blocks__title {% if block.settings.link_url != blank %}image-link-blocks__title--animate{% endif %} text-center bold">
                    {{- block.settings.link_text | escape -}}

                    {%- if block.settings.link_url != blank -%}
                      {%- if block.settings.stretch_image -%}
                        {% render 'icon' with 'circle-button-right-clipped', direction_aware: true %}
                      {%- else -%}
                        <span class="circle-chevron">{% render 'icon' with 'chevron-right-small', direction_aware: true %}</span>
                      {%- endif -%}
                    {%- endif -%}
                  </p>
                {%- endif -%}

              </a>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </image-link-blocks>

      <button is="prev-button" class="circle-button image-link-scallop-button" aria-controls="{{ scroll_area_id }}" disabled>
        <span class="sr-only">{{ 'general.accessibility.previous' | t }}</span>
        {%- render 'icon-arrow-left-scallop' -%}
      </button>

      <button is="next-button" class="circle-button image-link-scallop-button" aria-controls="{{ scroll_area_id }}">
        <span class="sr-only">{{ 'general.accessibility.next' | t }}</span>
        {%- render 'icon-arrow-right-scallop' -%}
      </button>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Image link blocks",
  "class": "shopify-section--image-link-blocks",
  "tag": "section",
  "disabled_on": {
    "templates": ["password"],
    "groups": ["header", "custom.overlay"]
  },
  "blocks": [
    {
      "type": "item",
      "name": "Link block",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image",
          "info": "1200 x 1400px .jpg recommended"
        },
        {
          "type": "image_picker",
          "id": "graphic",
          "label": "Graphic"
        },
        {
          "type": "image_picker",
          "id": "hover_graphic",
          "label": "Hover graphic"
        },
        {
          "type": "checkbox",
          "id": "stretch_image",
          "label": "Stretch to fill",
          "default": true
        },
        {
          "type": "text",
          "id": "link_text",
          "label": "Link text"
        },
        {
          "type": "richtext",
          "id": "link_subtext",
          "label": "Link subtext"
        },
        {
          "type": "url",
          "id": "link_url",
          "label": "Link URL"
        },
        {
          "type": "header",
          "content": "Colors",
          "info": "Overlay is ignored when \"Stretch to fill\" is disabled."
        },
        {
          "type": "color",
          "id": "background",
          "label": "Background"
        },
        {
          "type": "color",
          "id": "text_color",
          "label": "Text",
          "default": "#ffffff"
        },
        {
          "type": "color",
          "id": "overlay_color",
          "label": "Overlay",
          "default": "#000000",
          "visible_if": "{{ block.settings.stretch_image }}"
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "label": "Overlay opacity",
          "min": 0,
          "max": 100,
          "step": 1,
          "unit": "%",
          "default": 0,
          "visible_if": "{{ block.settings.stretch_image }}"
        },
        {
          "type": "color",
          "id": "hover_background_color",
          "label": "Hover background color",
          "default": "#F9CEC6"
        }
      ]
    }
  ],
  "settings": [
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "Full width",
      "default": false
    },
    {
      "type": "select",
      "id": "block_size",
      "label": "Block size",
      "options": [
        {
          "value": "small",
          "label": "Small"
        },
        {
          "value": "medium",
          "label": "Medium"
        },
        {
          "value": "large",
          "label": "Large"
        }
      ],
      "default": "medium"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Heading"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content"
    },
    {
      "type": "url",
      "id": "link_url",
      "label": "Link URL"
    },
    {
      "type": "text",
      "id": "link_text",
      "label": "Link text"
    },
    {
      "type": "header",
      "content": "Colors",
      "info": "Gradient replaces solid colors when set."
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background"
    },
    {
      "type": "color_background",
      "id": "background_gradient",
      "label": "Background gradient"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading color"
    },
    {
      "type": "color_background",
      "id": "heading_gradient",
      "label": "Heading gradient"
    }
  ],
  "presets": [
    {
      "name": "Image link blocks",
      "category": "Media",
      "blocks": [
        {
          "type": "item"
        },
        {
          "type": "item"
        },
        {
          "type": "item"
        },
        {
          "type": "item"
        }
      ]
    }
  ]
}
{% endschema %}